# Copiar a .env y completar antes de compilar (npm start / eas build / gradlew).
# Estos valores se inyectan en el bundle en build-time via react-native-dotenv (ver babel.config.js).
SUPABASE_URL=https://rvcdqsdtamldqtbgpeka.supabase.co
SUPABASE_ANON_KEY=
